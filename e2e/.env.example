# Copy to e2e/.env for local Playwright gauntlet (gitignored).
E2E_BASE=http://localhost:8081
E2E_USER=admin
E2E_PASSWORD=your-dev-password
# NC_ADMIN_USER / NC_ADMIN_PASS are accepted as aliases via tests/e2e/helpers/auth.js
